name: "Test entry without produce block"
definition: pipelines/test-filter.yaml
streams:
  source:
    topic: ksml_sensordata_avro
    keyType: string
    valueType: "avro:SensorData"
tests:
  no_produce:
    assert:
      - on: source
        code: |
          assert True
