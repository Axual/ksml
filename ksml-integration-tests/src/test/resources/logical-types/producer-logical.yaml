functions:
  generate_logical_message:
    type: generator
    globalCode: |
      counter = 0
    code: |
      global counter
      key = "sensor" + str(counter)
      counter = (counter + 1) % 10
      value = {
        "name": key,
        "id": "123e4567-e89b-12d3-a456-426614174000",   # uuid logical type, as a string
        "amount": "123.45",                              # decimal(10,2), as a canonical string
        "eventDate": 19785,                              # date, days since epoch
        "eventTimeMillis": 3723000,                      # time-millis, 01:02:03
        "eventTimestamp": 1700000000123,                 # timestamp-millis, fixed so the test can assert it
        "eventTimeMicros": 3723000000,                   # time-micros, 01:02:03
        "eventTimestampMicros": 1700000000123456,        # timestamp-micros
        "eventLocalTimestamp": 1700000000123,            # local-timestamp-millis
        "eventLocalTimestampMicros": 1700000000123456    # local-timestamp-micros
      }
    expression: (key, value)
    resultType: (string, json)

producers:
  logical_data_producer:
    generator: generate_logical_message
    interval: 3s
    to:
      topic: logical_data_avro
      keyType: string
      valueType: avro:LogicalData
